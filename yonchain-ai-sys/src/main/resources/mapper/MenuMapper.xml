<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yonchain.ai.sys.mapper.MenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.yonchain.ai.sys.entity.MenuEntity">
        <id column="id" property="id" jdbcType="OTHER"/>
        <result column="parent_id" property="parentId"/>
        <result column="weight" property="weight"/>
        <result column="name" property="name"/>
        <result column="path" property="path"/>
        <result column="sort" property="sort"/>
        <result column="menu_type" property="menuType"/>
        <result column="is_link" property="isLink"/>
        <result column="is_iframe" property="isIframe"/>
        <result column="is_keep_alive" property="isKeepAlive"/>
        <result column="icon" property="icon"/>
        <result column="en_name" property="enName"/>
        <result column="is_affix" property="isAffix"/>
        <result column="title" property="title"/>
        <result column="is_hide" property="isHide"/>
        <result column="permission" property="permission"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, parent_id, weight, name, path, is_link, is_iframe, is_keep_alive, icon, en_name, is_affix, title, is_hide, sort, menu_type, permission, created_at, updated_at
    </sql>

    <!-- 插入菜单 -->
    <insert id="insert" parameterType="com.yonchain.ai.sys.entity.MenuEntity">
        INSERT INTO sys_menu (
            id, parent_id, weight, name, path,
            is_link, is_iframe, is_keep_alive, icon, en_name, is_affix, title, is_hide,
            sort, menu_type, permission, created_at, updated_at
        ) VALUES (
            #{id}::uuid, #{parentId}::uuid, #{weight}, #{name}, #{path},
            #{isLink}, #{isIframe}, #{isKeepAlive}, #{icon}, #{enName}, #{isAffix}, #{title}, #{isHide},
            #{sort}, #{menuType}, #{permission}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <!-- 更新菜单 -->
    <update id="updateById" parameterType="com.yonchain.ai.sys.entity.MenuEntity">
        UPDATE sys_menu
        <set>
            <if test="parentId != null">parent_id = #{parentId}::uuid,</if>
            <if test="weight != null">weight = #{weight},</if>
            <if test="name != null">name = #{name},</if>
            <if test="path != null">path = #{path},</if>
            <if test="isLink != null">is_link = #{isLink},</if>
            <if test="isIframe != null">is_iframe = #{isIframe},</if>
            <if test="isKeepAlive != null">is_keep_alive = #{isKeepAlive},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="enName != null">en_name = #{enName},</if>
            <if test="isAffix != null">is_affix = #{isAffix},</if>
            <if test="title != null">title = #{title},</if>
            <if test="isHide != null">is_hide = #{isHide},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="menuType != null">menu_type = #{menuType},</if>
            <if test="permission != null">permission = #{permission},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}::uuid
    </update>

    <!-- 删除菜单 -->
    <delete id="deleteById" parameterType="java.lang.String">
        DELETE FROM sys_menu WHERE id = #{id}::uuid
    </delete>

    <!-- 根据ID查询菜单 -->
    <select id="selectById" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT <include refid="Base_Column_List"/> FROM sys_menu WHERE id = #{id}::uuid
    </select>

    <!-- 查询菜单列表 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM sys_menu
        <where>
            <if test="params != null and params.name != null and params.name != ''">
                AND name LIKE CONCAT('%', #{params.name}, '%')
            </if>
            <if test="params != null and params.menuType != null and params.menuType != ''">
                AND menu_type = #{params.menuType}
            </if>
            <if test="params != null and params.parentId != null and params.parentId != ''">
                AND parent_id = #{params.parentId}::uuid
            </if>
        </where>
        ORDER BY sort ASC, weight ASC
    </select>

    <!-- 查询用户关联的菜单列表 -->
    <select id="selectUserMenus" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT DISTINCT M.*
        FROM
            sys_user
                A JOIN sys_user_tenant taj ON A.ID = taj.account_id
                  JOIN sys_role r ON taj.ROLE = r.code and taj.tenant_id = r.tenant_id
                  JOIN sys_role_menu mr ON r.ID = mr.role_id
                  JOIN sys_menu M ON mr.menu_id = M.ID
        WHERE
            M.menu_type = #{menuType}
          AND A.ID = #{userId} :: UUID
          AND taj.tenant_id = #{tenantId} :: UUID
    </select>
</mapper>
